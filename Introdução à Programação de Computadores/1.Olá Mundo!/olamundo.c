#include <stdio.h> // Inclui a biblioteca padrão de entrada e saída (permite usar funções como printf)

int main() // Função principal: ponto de partida do programa
{
    printf("Olá, Mundo\n"); // Imprime a mensagem "Olá, Mundo" na tela.
                            // O "\n" quebra a linha no final do texto.

    return 0; // Retorna 0: indica que o programa terminou com sucesso
}
