#include <stdio.h> // Biblioteca padrão de entrada e saída

int main() // Função principal
{
    int idade;               // Declara uma variável inteira chamada "idade" (sem valor inicial)
    int quantidade = 1;      // Declara uma variável inteira já iniciada com valor 1
    float altura = 1.80;     // Número decimal de precisão simples (32 bits)
    double peso = 90.3;      // Número decimal de precisão dupla (64 bits, mais exato que float)
    char letra = 'D';        // Armazena um único caractere
    char nome[20] = "David"; // Array de 20 caracteres (string). Aqui, "David" ocupa parte dele.

    idade = 23; // Atribui valor à variável "idade"

    quantidade = 10; // Atualiza o valor da variável "quantidade"
}
